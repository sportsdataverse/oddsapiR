% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toa_sports_events_history.R
\name{toa_sports_events_history}
\alias{toa_sports_events_history}
\title{\strong{Find historical events for a sport through the Odds API}}
\usage{
toa_sports_events_history(
  sport_key,
  date,
  date_format = "iso",
  event_ids = NULL,
  commence_time_from = NULL,
  commence_time_to = NULL,
  include_rotation_numbers = FALSE
)
}
\arguments{
\item{sport_key}{(\emph{string}, required): The \code{sport_key} to look up events for.
See \code{\link[=toa_sports]{toa_sports()}} for a full lookup of \code{sport_key} values.}

\item{date}{(\emph{string}, required): The timestamp of the data snapshot to
return, in ISO 8601 format (e.g. \verb{2023-10-10T12:15:00Z}). The API returns
the closest snapshot equal to or earlier than this value.}

\item{date_format}{(\emph{string}, optional): Format of returned timestamps.
Options are \code{iso} (default) or \code{unix}.}

\item{event_ids}{(\emph{string}, optional): Comma-separated list of event ids to
filter the response to. Defaults to \code{NULL} (all events).}

\item{commence_time_from}{(\emph{string}, optional): Filter to events that
commence on and after this ISO 8601 timestamp.}

\item{commence_time_to}{(\emph{string}, optional): Filter to events that commence
on and before this ISO 8601 timestamp.}

\item{include_rotation_numbers}{(\emph{logical}, optional): If \code{TRUE}, include the
\code{home_rotation} and \code{away_rotation} columns when available. Defaults to
\code{FALSE}.}
}
\value{
A tibble with one row per historical event:\tabular{lll}{
   col_name \tab types \tab description \cr
   timestamp \tab character \tab Snapshot timestamp returned (closest at/before \code{date}). \cr
   previous_timestamp \tab character \tab Preceding available snapshot; use as \code{date} to page back. \cr
   next_timestamp \tab character \tab Next available snapshot; use as \code{date} to page forward. \cr
   id \tab character \tab Unique event id. Use as \code{event_id} elsewhere. \cr
   sport_key \tab character \tab Sport key, e.g. \code{basketball_nba}. \cr
   sport_title \tab character \tab Human-readable sport title, e.g. \code{NBA}. \cr
   commence_time \tab character \tab Event start time (ISO 8601 string or unix seconds). \cr
   home_team \tab character \tab Home team name. \cr
   away_team \tab character \tab Away team name. \cr
   home_rotation \tab integer \tab Home rotation number (only when \code{include_rotation_numbers = TRUE}). \cr
   away_rotation \tab integer \tab Away rotation number (only when \code{include_rotation_numbers = TRUE}). \cr
}
}
\description{
\strong{Get the list of events for a sport as they appeared at a point in time.}

Returns the events (without odds) that existed at the snapshot closest to
(and at or before) the requested \code{date}. The returned \code{timestamp},
\code{previous_timestamp} and \code{next_timestamp} columns let you page through
snapshots. The \code{id} returned here is the \code{event_id} consumed by
\code{\link[=toa_event_odds_history]{toa_event_odds_history()}}.

Historical data is available on paid usage plans from June 2020.

\strong{Usage quota cost:} 1 credit (0 if no events are found at the snapshot).
}
\examples{
\donttest{
   try(toa_sports_events_history(sport_key = 'basketball_nba',
                                 date = '2024-01-15T12:15:00Z'))
}
}
\seealso{
\code{\link[=toa_event_odds_history]{toa_event_odds_history()}} to pull odds for a historical event by
its \code{id}, \code{\link[=toa_sports_odds_history]{toa_sports_odds_history()}} for featured-market odds at a
historical snapshot, and \code{\link[=toa_sports_events]{toa_sports_events()}} for current events. Part of
the \href{https://sportsdataverse.org/}{SportsDataverse}.

Other The Odds API: Historical: 
\code{\link{toa_event_odds_history}()},
\code{\link{toa_sports_odds_history}()}
}
\concept{The Odds API: Historical}
\keyword{Events}
