% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toa_event_odds.R
\name{toa_event_odds}
\alias{toa_event_odds}
\title{\strong{Find odds for a single event, including player props and other markets}}
\usage{
toa_event_odds(
  sport_key,
  event_id,
  regions = "us",
  markets = "spreads",
  odds_format = "decimal",
  date_format = "iso",
  bookmakers = NULL
)
}
\arguments{
\item{sport_key}{(\emph{string}, required): The \code{sport_key} to look up odds for.
See \code{\link[=toa_sports]{toa_sports()}} for a full lookup of \code{sport_key} values.}

\item{event_id}{(\emph{string}, required): The \code{event_id} to look up odds for.
See \code{\link[=toa_sports_events]{toa_sports_events()}} for a full lookup of \code{event_id} values.}

\item{regions}{(\emph{string}, required): The region(s) to pull bookmakers from.
Multiple can be specified if comma delimited. Options include:
\itemize{
\item \code{us}
\item \code{us2}
\item \code{uk}
\item \code{eu}
\item \code{au}
}}

\item{markets}{(\emph{string}, optional): The markets to return. Multiple can be
specified if comma delimited. In addition to \code{h2h}, \code{spreads}, \code{totals} and
\code{outrights}, this endpoint accepts additional markets such as alternate
lines and player props (e.g. \code{player_points}, \code{player_pass_tds},
\code{player_shots_on_goal}). See the
\href{https://the-odds-api.com/sports-odds-data/betting-markets.html}{betting markets documentation}
for the full list.}

\item{odds_format}{(\emph{string}, optional): Format in which to return odds.
Options are \code{decimal} (default) or \code{american}.}

\item{date_format}{(\emph{string}, optional): Format of returned timestamps.
Options are \code{iso} (default) or \code{unix}.}

\item{bookmakers}{(\emph{string}, optional): Comma-separated list of bookmakers to
be returned. If both \code{bookmakers} and \code{regions} are specified, \code{bookmakers}
takes precedence. Bookmakers can be from any region. Every group of 10
bookmakers counts as 1 region against the usage quota.}
}
\value{
A long-format tibble with one row per bookmaker market outcome:\tabular{lll}{
   col_name \tab types \tab description \cr
   id \tab character \tab Unique event id (echoes \code{event_id}). \cr
   sport_key \tab character \tab Sport key, e.g. \code{basketball_nba}. \cr
   sport_title \tab character \tab Human-readable sport title, e.g. \code{NBA}. \cr
   commence_time \tab character \tab Game start time (ISO 8601 string or unix seconds). \cr
   home_team \tab character \tab Home team name. \cr
   away_team \tab character \tab Away team name. \cr
   bookmaker_key \tab character \tab Bookmaker slug, e.g. \code{draftkings}. \cr
   bookmaker \tab character \tab Bookmaker display title, e.g. \code{DraftKings}. \cr
   market_key \tab character \tab Market key, e.g. \code{player_points}. \cr
   market_last_update \tab character \tab When this market's odds were last updated. \cr
   outcomes_name \tab character \tab Outcome label (team, \code{Over}/\code{Under}, player name, etc.). \cr
   outcomes_description \tab character \tab Outcome description (player name for props); absent for featured markets. \cr
   outcomes_price \tab numeric \tab The price/odds for the outcome. \cr
   outcomes_point \tab numeric \tab The handicap/total/prop line, when applicable. \cr
}


If the event exists but no bookmaker odds are posted yet for the requested
markets/regions, a zero-row tibble with the same columns is returned.
}
\description{
\strong{Get bookmaker odds for a single event from The Odds API.}

Unlike \code{\link[=toa_sports_odds]{toa_sports_odds()}} (featured markets only), this endpoint accepts any
available market key, including player props and alternate lines. Look up an
\code{event_id} with \code{\link[=toa_sports_events]{toa_sports_events()}}.

\strong{Usage quota cost:} \verb{[number of unique markets returned] x [number of regions]}.
}
\examples{
\donttest{
   try(toa_event_odds(sport_key = 'basketball_nba',
                      event_id = '48db9c3293a52baab881d95d38f37a98',
                      regions = 'us',
                      markets = 'player_points',
                      odds_format = 'decimal',
                      date_format = 'iso'))
}
}
\seealso{
\code{\link[=toa_sports_odds]{toa_sports_odds()}} for featured-market odds across all upcoming
events, \code{\link[=toa_event_markets]{toa_event_markets()}} to discover available market keys for an
event, and \code{\link[=toa_sports_events]{toa_sports_events()}} to look up \code{event_id} values. Part of
the \href{https://sportsdataverse.org/}{SportsDataverse}.

Other The Odds API: Odds & Markets:
\code{\link[=toa_event_markets]{toa_event_markets()}},
\code{\link[=toa_sports_odds]{toa_sports_odds()}}
}
\concept{The Odds API: Odds & Markets}
\keyword{Betting}
\keyword{Lines}
