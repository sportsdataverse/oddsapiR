% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toa_sports_events.R
\name{toa_sports_events}
\alias{toa_sports_events}
\title{\strong{Find in-play and upcoming events for a sport through the Odds API}}
\usage{
toa_sports_events(
  sport_key,
  date_format = "iso",
  event_ids = NULL,
  commence_time_from = NULL,
  commence_time_to = NULL,
  include_rotation_numbers = FALSE
)
}
\arguments{
\item{sport_key}{(\emph{string}, required): The \code{sport_key} to look up events for.
See \code{\link[=toa_sports]{toa_sports()}} for a full lookup of \code{sport_key} values.}

\item{date_format}{(\emph{string}, optional): Format of returned timestamps.
Options are \code{iso} (default) or \code{unix}.}

\item{event_ids}{(\emph{string}, optional): Comma-separated list of event ids to
filter the response to. Defaults to \code{NULL} (all events).}

\item{commence_time_from}{(\emph{string}, optional): Filter to events that
commence on and after this ISO 8601 timestamp (e.g. \verb{2023-09-09T00:00:00Z}).}

\item{commence_time_to}{(\emph{string}, optional): Filter to events that commence
on and before this ISO 8601 timestamp (e.g. \verb{2023-09-09T00:00:00Z}).}

\item{include_rotation_numbers}{(\emph{logical}, optional): If \code{TRUE}, include the
\code{home_rotation} and \code{away_rotation} columns when available. Defaults to
\code{FALSE}.}
}
\value{
A tibble with one row per event:\tabular{lll}{
   col_name \tab types \tab description \cr
   id \tab character \tab Unique event id. Use as \code{event_id} elsewhere. \cr
   sport_key \tab character \tab Sport key, e.g. \code{basketball_nba}. \cr
   sport_title \tab character \tab Human-readable sport title, e.g. \code{NBA}. \cr
   commence_time \tab character \tab Event start time (ISO 8601 string or unix seconds). \cr
   home_team \tab character \tab Home team name. \cr
   away_team \tab character \tab Away team name. \cr
   home_rotation \tab integer \tab Home rotation number (only when \code{include_rotation_numbers = TRUE}). \cr
   away_rotation \tab integer \tab Away rotation number (only when \code{include_rotation_numbers = TRUE}). \cr
}
}
\description{
\strong{Get the list of in-play and pre-match events for a sport.}

Returns a list of events without odds. The \code{id} returned here is the
\code{event_id} consumed by \code{\link[=toa_event_odds]{toa_event_odds()}} and \code{\link[=toa_event_markets]{toa_event_markets()}}.

\strong{Usage quota cost:} Free. This endpoint does not count against your quota.
}
\examples{
\donttest{
   try(toa_sports_events(sport_key = 'basketball_nba'))
}
}
\seealso{
\code{\link[=toa_sports]{toa_sports()}} to discover \code{sport_key} values, \code{\link[=toa_event_odds]{toa_event_odds()}}
to pull odds for a specific event by its \code{id}, and \code{\link[=toa_event_markets]{toa_event_markets()}}
to see available market keys per bookmaker. Part of the
\href{https://sportsdataverse.org/}{SportsDataverse}.

Other The Odds API: Sports & Events:
\code{\link[=toa_sports]{toa_sports()}},
\code{\link[=toa_sports_participants]{toa_sports_participants()}},
\code{\link[=toa_sports_scores]{toa_sports_scores()}}
}
\concept{The Odds API: Sports & Events}
\keyword{Events}
